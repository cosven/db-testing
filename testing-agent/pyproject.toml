[project]
name = "testing-agent"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
  "click>=8.1",
  "pymysql>=1.1",
  "requests>=2.31",
]

[build-system]
requires = ["hatchling>=1.18"]
build-backend = "hatchling.build"
